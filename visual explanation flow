Developer Commit
        ↓
Secrets Scan (Gitleaks)
        ↓
SAST Scan (Semgrep)
        ↓
Dependency Scan (Safety + pip-audit)
        ↓
Filesystem Scan (Trivy FS)
        ↓
Docker Build
        ↓
Container CVE Scan (Trivy Image)
        ↓
Runtime DAST Scan (OWASP ZAP)
        ↓
Security Gate Decision
   ↙              ↘
Dev Policy       Prod Policy
Allowed ✅       Blocked ❌

Visual instance architecture-level
( I implemented two security gate policies —
a relaxed threshold for development validation and a strict zero-tolerance threshold for production deployment control
— demonstrating environment-aware release governance inside the DevSecOps pipeline.)
